# Compatibility path

The public product and canonical release path are now
[Synapse Agent Memory](../synapse-agent-memory/README.md).

Only the two installer forwarders remain here so links from the earlier
`synapse-memory-v*` candidates do not strand users. New code, documentation,
packaging, tags, and assets use `synapse-agent-memory`.
